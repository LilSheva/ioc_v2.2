# Инструкция по использованию "IOC Parser"

**IOC Parser** — это утилита для специалистов по информационной безопасности, предназначенная для автоматического извлечения Индикаторов Компрометации (IOC) из документов Word (`.docx`), генерации Excel-отчетов и готовых поисковых запросов для SIEM и NAD -систем.

---

## 1. Быстрый старт: Как извлечь IOC за 5 шагов

Основная работа происходит на вкладке **`🏠 Главная`**.

1.  **Добавьте файлы:**
    *   Нажмите на кнопку **`📁 Добавить файлы...`**.
    *   Выберите один или несколько документов в формате `.docx`. Выбранные файлы появятся в списке.

2.  **Укажите бюллетень (опционально):**
    *   Если отчет относится к конкретному бюллетеню или инциденту, впишите его номер или название в поле **`Бюллетень`**. Это значение появится в соответствующем столбце Excel-отчета.

3.  **Сформируйте отчеты:**
    *   Нажмите на большую зеленую кнопку **`⚡ Сформировать отчеты`**.
    *   Программа предложит вам выбрать место для сохранения и имя для `.xlsx` отчета.
    *   Рядом с `.xlsx` отчетом будет автоматически создан текстовый файл с поисковыми запросами (`имя_отчета_queries.txt`).

4.  **Изучите лог:**
    *   Весь процесс работы, включая количество найденных IOC по типам, отображается в реальном времени в окне **`Журнал работы`**.

5.  **Скопируйте запросы:**
    *   Перейдите на вкладку **`📊 Результаты запросов`**.
    *   Здесь все сгенерированные поисковые запросы сгруппированы по типу IOC.
    *   Нажмите кнопку **`📋 Копировать`** напротив нужного запроса, чтобы скопировать его в буфер обмена для вставки в вашу SIEM или другую систему.

---

## 2. Настройка через интерфейс

Вкладка **`⚙️ Настройка IOC`** позволяет быстро редактировать основные параметры парсинга без прямого редактирования файла `config.txt`.

*   **Включение/Отключение:** Используйте переключатель `✓ Включено` для каждого типа IOC.
*   **Приоритет в отчете:** Кнопками `▲` и `▼` можно изменять порядок, в котором группы IOC (IP, DNS, URI и т.д.) будут расположены в итоговом `.xlsx` отчете.
    > **Важно:** Это не влияет на порядок *извлечения* индикаторов из документа (он всегда одинаковый для максимальной точности), а только на их *визуальный порядок* в финальном файле.
*   **Редактирование полей:** Вы можете менять значения в полях `Тип в отчете` и `Статус NTA/SIEM`.
*   **Сохранение:** После внесения изменений обязательно нажмите **`💾 Сохранить все настройки`**.

> **Примечание:** Редактирование регулярных выражений, шаблонов запросов и списков исключений доступно только через файл `config.txt`.

---

## 3. Продвинутая настройка через `config.txt`

Файл `config.txt` (в формате JSON) дает полный контроль над логикой парсера.

**Внимание!** При редактировании файла строго соблюдайте синтаксис JSON: следите за запятыми в конце строк, кавычками и скобками.

### Структура блока IOC

Каждый тип IOC описывается блоком со следующими полями:

*   `"enabled"`: `true` или `false`. Включает или отключает поиск этого типа IOC.
*   `"name"`: Внутреннее имя типа (например, `"IP"`, `"DNS"`). **Не рекомендуется менять.**
*   `"regex"`: Регулярное выражение для поиска индикатора. **Редактировать с осторожностью!**
*   `"report_type"`: Название, которое будет отображаться в столбце "Тип Индикатора" в Excel-отчете (например, `"IP-адрес"`).
*   `"nta_status"`, `"siem_tools_status"`, `"siem_status"`: Текстовые значения по умолчанию для столбцов со статусами в отчете.
*   `"mp10_templates"`, `"nad_templates"`: Массивы (списки) шаблонов для генерации поисковых запросов. `{ioc}` будет автоматически заменен на найденный индикатор.

### Особенности настройки для IOC типа "File"

Блок для `File` имеет два дополнительных поля для тонкой фильтрации:

*   `"file_blacklist"`: **Список контекстных слов-исключений.** Если одно из этих слов находится в тексте непосредственно **ПЕРЕД** именем файла в кавычках, то этот файл не будет добавлен в отчет.
    *   *Пример:* `...с тематикой «черный.pdf»`. Если в списке есть `"тематикой"`, то `черный.pdf` будет проигнорирован.

*   `"filename_exclusions"`: **Список точных имен файлов для исключения.** Если найденное имя файла в кавычках полностью совпадает с одним из имен в этом списке, оно не будет добавлено в отчет.
    *   *Пример:* `«1.docx»`. Если в списке есть `"1.docx"`, этот файл будет проигнорирован. Идеально подходит для фильтрации общих или неинформативных имен файлов.
